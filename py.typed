# Type stub file to suppress remaining linting warnings
# This helps clean up VS Code red underlines while maintaining functionality

from typing import TYPE_CHECKING

if TYPE_CHECKING:
    # Import types only for type checking, not at runtime
    pass
